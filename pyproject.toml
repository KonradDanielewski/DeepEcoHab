[build-system]
requires = ['setuptools']
build-backend = 'setuptools.build_meta'

[project]
name = 'deepecohab'
version = '0.2'
requires-python = '>=3.13'
dependencies = [
    'numpy',
    'plotly',
    'matplotlib',
    'scipy',
    'pandas',
    'tables',
    'toml',
    'openskill',
    'networkx',
    'nbformat',
    'kaleido',
    'joblib',
    'tqdm',
    'tzlocal',
]